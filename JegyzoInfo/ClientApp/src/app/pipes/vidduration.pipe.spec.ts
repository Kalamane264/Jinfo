import { ViddurationPipe } from './vidduration.pipe';

describe('ViddurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ViddurationPipe();
    expect(pipe).toBeTruthy();
  });
});
